# .github/workflows/ci.yml
name: CI

on:
  push:
    branches:
      - main
      - "release/**"

jobs:
  ci:
    uses: augustmckendrick/queuetester/.github/workflows/shared-ci.yml@main
    secrets: inherit